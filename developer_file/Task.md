Current Task is to build a local mvp version of this app.
Everything should store in local.
the app direct call and receive LLM using api key.
screen jump route: 
1. First of all there are three main section, Meal plan, shopping and Setting, The bottom navigation bar should always showing and click able and for user to switch section to view. Each section should feel like independent so that switching section will not effect the current page in each section.
2. Meal plan section has page:
    a. "Meal planning": this page for user views existing meal plan, upper half screen is calender, lower half screen is all day meal plan for the selected day. Each recipe under that meal is clickable and jump to that "Recipe card detail" page. There is also a “New Plan” button jump to "Preference collection" page.
    b. "Recipe card detail": This page just show the detail of a cooking recipe. it has a go back button at top left to whatever earlier page, and a share button at top right for social media or save img in future.
    c. "Preference collection": This page have a calender at upper half of the screen also a budget bar and meals per day button at top, so user can swipe on the calender to select dates need meal planning, and the lower half of the screen is a text enter box to collect more detailed prompt for send to LLM later. around text box there is also a floating mic button for voice input. At the bottom but above navigation there is a Done button jump to "New Meals" page. 
    d. "New meals": this page shows the meal plan just generated by LLM and somehow transfered into each meal unit, and under each meal unit there are one or more recipes. Loading or receiving answer from LLM might be long so this page need a loading animation. The meal and recipe structer in this page should looks very same with the lower half of "Meal planning" page, but on the right of each recipe there is a mark button for user to select unwanted dish. And At bottom left above navigation the "regenerate" button for reproduct the unwanted dish (will send a request to LLM again and receive new recipes to refill). At bottom right the Done buttom will jump to shopping section "Cart" page and also load all recipe required and valid(sell by our markert) ingredients into user's cart.
3. Shop section has page:
    a. "Shopping": this page is like most common shopping app but be aware that this page is like a tree: it list of many category of products, click each category might show a list of sub category, or a list of products (so we need to create and store a example product tree for the mvp, the real app will request product structure or list from server) with price and "add to cart" button. If user click the prodcut picture, it jump to "Product detail" page. There is also floating "Cart" button all the time for user jump to "View cart" page.
    b. "Product detail": this page is like most common product detail page, it show product name, picture, price per unit, quantity, description, and "add to cart" button. There is also a floating "Cart" button all the time for user jump to "View cart" page. At top left there is a back button for go back to earlier page, and at top right there is a Share button for share to social media or save img.
    c. "View cart": this page is a typical user's shopping cart, it show all the selected product list, each item should shows product picture, name, price, unit, quantity and adjust buttom(plus 1 or minus 1). At the end of list there is a Delivery part shows which address the user want to do delivery. And there is a "Total price" summary below address. At bottom there is a "check out" button for jump to triggre pay fucntion and will goto "Receipt" page.
    d. "Receipt": regular receipt page, just follow figma design.
4. Setting section has page:
    a. "Setting": regular app setting page, just follow figma design.

5. All the UI page above have corresponding figma design already, you should create those pages or UI vary similar to the desgin. You read the desgin through Figma MCP.
 

**Task1**
The Very First Task is to build the mvp of this app. Through Figma MCP https://www.figma.com/design/ubtKvIGSMUhykXHE44GxHL/MealPlanner?node-id=0-1&p=f&t=VG5xf3V2Rie3iCGW-0 , you can see 9 mian screen/page of this app are already desgined by human. You should create this app's structure depend on the design.
Including :
1. Meal planning
2. Recipe card detail
3. Preference collection
4. New meals
5. Shopping
6. Product detail
7. View cart
8. Receipt
9. Setting
In this task just create those screen/pages, and you do not need to create recipe generate functions or call LLM API. you do not need to create any function related to user authentication. You do not need to build real recipe to cart function and pay function, everything of those can use preset example just for demostration. But you do need most page jump button or selecting button work, you do need to build class like "recipe card detail" or "product" or "delivery" somehow in this app because obviously those are common program knowledge and might have alot or repeatly using in this app in future.
